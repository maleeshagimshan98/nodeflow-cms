/**
 * Main app instance test file
 */

/**
 * app is setup with all controllers, middlewares, libraries and running
 */

const app = require("../src/App/app.js")

//... test for all the controllers, models, middlewares, storage drivers are present